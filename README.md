# Better Together — GitHub Shared Final

This package is for the existing Better Together GitHub Pages site.

## GitHub
Replace only:
- `app.js`

The included `index.html`, `styles.css`, and `assets/` are copies of the existing v3.1 front end for reference and are visually unchanged.

## Google Apps Script
Use:
- `apps-script/Code.gs`

`Bridge.html` is no longer needed.

See `SETUP.md` for the exact setup sequence.
